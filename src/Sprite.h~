#ifndef SPRITE_H_
#define SPRITE_H_

#include <GL/glew.h>

class Sprite
{
public:
	Sprite();
	~Sprite();

	void init(float x, float y, float width, float height);

	void draw();

private:
	int _x;
	int _y;
	int _width;
	int _height;
	GLuint _vboID; //vertex buffer object ID

};

#endif
