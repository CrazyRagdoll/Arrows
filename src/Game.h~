#ifndef GAME_H_
#define GAME_H_

#include <SDL2/SDL.h>
#include <GL/glew.h>
#include <GL/gl.h>

class Game
{
public:
	Game();
	~Game();

	void run();

	void initSystems();

private:
	SDL_Window* _window;
	int _screenWidth;
	int _screenHeight;	
};

#endif
